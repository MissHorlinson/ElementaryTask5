import controller.NumberController;

public class Main {
    public static void main(String[] args) {
        while (true) {
            NumberController controller = new NumberController();
            controller.takeParts();
        }
    }
}
